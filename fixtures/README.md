# Fixtures

This directory stores shared fixtures for:

- transcript capture and ordering
- provider responses
- ticket-generation parity
- seeded search datasets

These fixtures are intentionally separate from `ticket-generator/` so the reference implementation can remain unchanged while parity tests are added for the desktop app.
