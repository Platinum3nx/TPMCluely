{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for Cluely Desktop foundation commands.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
